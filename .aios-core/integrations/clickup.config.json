{
    "name": "clickup",
    "type": "api",
    "status": "pending",
    "credentials_ref": ".env → CLICKUP_API_TOKEN",
    "purpose": "Gestão de tarefas da agência Experia. Tracking de entregas por cliente, sprints, e backlog.",
    "agents_with_access": [
        "EXPERIA-MASTER",
        "ESTRATEGISTA-EXPERIA"
    ],
    "mcp_tool": null,
    "test_command": "curl -s -H 'Authorization: ${CLICKUP_API_TOKEN}' https://api.clickup.com/api/v2/user",
    "notes": "API Token gerado em clickup.com → Settings → Apps. Free tier suporta todas as features necessárias."
}