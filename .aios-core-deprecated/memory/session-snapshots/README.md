# Session Snapshots Directory

> Store 280-token compressed context snapshots here after each LLM session.
> Format: `YYYY-MM-DD.md` (one per day, append if multiple sessions)
> See `session-protocol.md` for snapshot generation instructions.
